# Website TODO

Planning notes for the site. Editable from nb as `website/TODO` — this file lives in
`src/content/` but outside `posts/`/`projects/`, so it is never built or served.

## Active — order interchangeable

- [ ] **Rework the projects section GUI** to fit my own vision — the `/projects` index
      (cards + category tabs) and the `/projects/[slug]` layout are currently just a
      functional placeholder.
- [ ] **Full writeup of a course project** — standalone, written after the fact. New
      `src/content/projects/<slug>.md` with `published: true`. No dev logs by design, so
      it simply won't render a "dev log" section (leave `project:` off; nothing links to
      it).

## Someday / ideas

<!-- park future ideas here -->
