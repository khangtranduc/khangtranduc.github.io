---
title: 'Example dev log #4: amongusing it up'
description: Fourth entry of the example dev-log series — tying it off.
date: 2026-07-04
published: true
tags:
  - example
  - cs
# A post can belong to more than one project. Use `projects:` (a list)
# instead of `project:` when that happens, e.g.:
#   projects:
#     - example-project
#     - some-other-project
project: example-project
---

Final entry of the example series. Once the project is done, you'd flip the
project's `status` to `complete` and write the polished story in
`example-project.md` — these dev logs stay as the running history behind it.

The comment in this file's frontmatter also shows the `projects:` list form, for
the rare post that belongs to two projects at once.
